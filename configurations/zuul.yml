
server:
  port: 9002

security.basic.enabled: false

logging.level:
  ru.xxlabaza.test.pcj.zuul.ribbon.predicate: DEBUG

zuul:
  ignoredServices: '*'
  prefix: /api
  routes:
    facade: /facade/**


app:
  metadata:
    balancing:
      rules:
        facade:
          '[1.0.0]': 0
          # '[null]': 100
